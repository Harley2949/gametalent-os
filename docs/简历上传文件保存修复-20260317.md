# 简历上传文件保存修复 - 20260317

## 问题诊断

用户上传简历后出现以下问题：
1. ✅ 候选人记录创建成功
2. ❌ 所有字段为空（姓名=待完善、邮箱=mailto:temp_xxx@example.com）
3. ❌ 简历预览区显示"待上传简历"，文件没关联上
4. ❌ 数据库中 Resume 表的 filePath 为 null

## 根本原因

**Multer 配置缺少磁盘存储设置**，文件只存储在内存中（buffer），没有保存到磁盘。

### 问题代码（resume-upload.module.ts）
```typescript
MulterModule.register({
  limits: {
    fileSize: 5 * 1024 * 1024, // 5MB
  },
  // ❌ 缺少 dest 配置！文件只存储在内存中
})
```

### 问题代码（resume-upload.service.ts）
```typescript
const resume = await this.prisma.resume.create({
  data: {
    // ...
    fileUrl: `/uploads/resumes/${data.file.originalname}`, // ❌ 文件实际上不存在
    // ...
  },
});
```

## 修复方案

### 1. 修复 Multer 配置 ✅

**文件**: [resume-upload.module.ts](../apps/api/src/modules/resume-upload/resume-upload.module.ts)

```typescript
import * as path from 'path';
import * as fs from 'fs';

// 确保 uploads 目录存在
const uploadsDir = path.join(process.cwd(), 'uploads', 'resumes');
if (!fs.existsSync(uploadsDir)) {
  fs.mkdirSync(uploadsDir, { recursive: true });
  console.log(`✅ 创建上传目录: ${uploadsDir}`);
}

@Module({
  imports: [
    MulterModule.register({
      dest: uploadsDir, // ✅ 设置文件保存目录
      limits: {
        fileSize: 5 * 1024 * 1024, // 5MB
      },
      fileFilter: (req, file, callback) => {
        // ... file filter logic
      },
    }),
    // ...
  ],
})
export class ResumeUploadModule {}
```

**改进点**：
- ✅ 添加 `dest` 配置，指定文件保存目录
- ✅ 自动创建 uploads 目录（如果不存在）
- ✅ 启动时打印目录创建日志

### 2. 增强文件保存逻辑 ✅

**文件**: [resume-upload.service.ts](../apps/api/src/modules/resume-upload/resume-upload.service.ts#L353-L424)

```typescript
private async saveResume(data: {
  candidateId: string;
  file: any;
  fileContent: string;
  parsedData: ParsedResumeResult;
  title?: string;
  isPrimary: boolean;
}) {
  this.logger.log('📁 开始保存简历文件...', {
    originalname: data.file.originalname,
    mimetype: data.file.mimetype,
    size: data.file.size,
    hasBuffer: !!data.file.buffer,
    hasPath: !!data.file.path,
  });

  // 确定文件路径
  let filePath = data.file.path; // Multer 保存的路径
  let fileUrl = `/uploads/resumes/${data.file.filename || data.file.originalname}`;

  // ✅ 如果文件在内存中（buffer），手动保存到磁盘
  if (!filePath && data.file.buffer) {
    const fs = await import('fs');
    const path = await import('path');

    const uploadsDir = path.join(process.cwd(), 'uploads', 'resumes');
    const uniqueFileName = `${Date.now()}-${data.file.originalname}`;
    filePath = path.join(uploadsDir, uniqueFileName);
    fileUrl = `/uploads/resumes/${uniqueFileName}`;

    this.logger.log(`💾 手动保存文件到磁盘: ${filePath}`);

    try {
      fs.writeFileSync(filePath, data.file.buffer);
      this.logger.log(`✅ 文件保存成功: ${filePath}`);
    } catch (error) {
      this.logger.error('❌ 文件保存失败:', error);
      // 即使文件保存失败，仍然继续创建数据库记录
      filePath = '';
      fileUrl = '';
    }
  }

  // ✅ 检查文件是否存在
  if (filePath) {
    const fs = await import('fs');
    const fileExists = fs.existsSync(filePath);
    this.logger.log(`🔍 文件存在检查: ${filePath} - ${fileExists ? '存在' : '不存在'}`);

    if (!fileExists) {
      this.logger.warn(`⚠️ 警告：文件路径不存在 ${filePath}`);
    }
  }

  const resume = await this.prisma.resume.create({
    data: {
      candidateId: data.candidateId,
      title: data.title || data.file.originalname,
      fileName: data.file.originalname,
      fileUrl: fileUrl, // ✅ 使用实际的文件路径
      fileSize: data.file.size,
      fileType: data.file.mimetype,
      rawData: data.fileContent,
      rawText: data.fileContent,
      parsedData: data.parsedData as any,
      skills,
      status: ResumeStatus.READY,
      parsedAt: new Date(),
      parserVersion: '1.0.0',
      isPrimary: data.isPrimary,
    },
  });

  this.logger.log(`✅ 创建简历记录成功`, {
    resumeId: resume.id,
    fileUrl: resume.fileUrl,
    filePath: filePath,
    fileName: resume.fileName,
  });

  return resume;
}
```

**改进点**：
- ✅ 详细的调试日志（文件信息、保存状态、存在性检查）
- ✅ 支持内存 buffer 和磁盘文件两种模式
- ✅ 使用时间戳生成唯一文件名，避免覆盖
- ✅ 文件保存失败时降级处理（仍然创建数据库记录）
- ✅ 文件存在性验证

### 3. 添加静态文件服务 ✅

**文件**: [main.ts](../apps/api/src/main.ts#L50-L55)

```typescript
// 静态文件服务 - 提供上传的文件访问
const uploadsPath = require('path').join(process.cwd(), 'uploads');
app.use('/uploads', express.static(uploadsPath));
console.log(`✅ 静态文件服务已启用: /uploads -> ${uploadsPath}`);
```

**改进点**：
- ✅ 通过 `/uploads` 路径提供静态文件访问
- ✅ 前端可以通过 `http://localhost:3006/uploads/resumes/xxx.pdf` 访问文件

### 4. 增强调试日志 ✅

**文件**: [resume-upload.service.ts](../apps/api/src/modules/resume-upload/resume-upload.service.ts#L96-L142)

```typescript
private async extractFileContent(file: any): Promise<string> {
  this.logger.log('📄 开始提取文件内容...', {
    originalname: file.originalname,
    mimetype: file.mimetype,
    size: file.size,
    hasBuffer: !!file.buffer,
    hasPath: !!file.path,
    bufferLength: file.buffer?.length || 0,
  });

  try {
    const buffer = file.buffer;

    if (!buffer) {
      throw new Error('文件 buffer 为空');
    }

    // PDF 文件
    if (file.mimetype === 'application/pdf') {
      this.logger.log('📕 检测到 PDF 文件，开始解析...');
      const data = await pdf(buffer);
      const textLength = data.text.length;
      this.logger.log(`✅ PDF 解析成功，提取文本长度: ${textLength} 字符`);
      return data.text;
    }

    // 图片文件 - 返回占位符（需要 OCR 服务）
    if (file.mimetype.startsWith('image/')) {
      this.logger.warn('⚠️ 图片文件需要 OCR 服务，当前返回占位符');
      return '[图片文件 - 需要 OCR 解析]';
    }

    throw new BadRequestException('不支持的文件类型');
  } catch (error) {
    this.logger.error('❌ 提取文件内容时出错:', {
      error: error instanceof Error ? error.message : String(error),
      filename: file.originalname,
      mimetype: file.mimetype,
    });
    throw new BadRequestException('文件解析失败，请确保文件格式正确');
  }
}
```

**改进点**：
- ✅ 文件信息详细日志
- ✅ PDF 解析进度跟踪
- ✅ 错误详情记录

### 5. 创建目录结构 ✅

```bash
mkdir -p apps/api/uploads/resumes
```

## 验证步骤

### 1. 检查后端日志

上传简历后应该看到以下日志：

```
📁 开始保存简历文件... { originalname: 'resume.pdf', mimetype: 'application/pdf', size: 123456, hasBuffer: true, hasPath: false }
💾 手动保存文件到磁盘: C:\...\uploads\resumes\1710672000000-resume.pdf
✅ 文件保存成功: C:\...\uploads\resumes\1710672000000-resume.pdf
🔍 文件存在检查: C:\...\uploads\resumes\1710672000000-resume.pdf - 存在
✅ 创建简历记录成功 { resumeId: 'xxx', fileUrl: '/uploads/resumes/1710672000000-resume.pdf', ... }
```

### 2. 检查文件系统

```bash
ls -la apps/api/uploads/resumes/
# 应该看到上传的文件
```

### 3. 检查数据库

```sql
SELECT id, "fileName", "fileUrl", "fileSize"
FROM resumes
ORDER BY "createdAt" DESC
LIMIT 1;
```

应该看到：
- `fileName` = 原始文件名
- `fileUrl` = `/uploads/resumes/时间戳-文件名`
- `fileSize` = 实际文件大小

### 4. 测试文件访问

```bash
curl -I http://localhost:3006/uploads/resumes/1710672000000-resume.pdf
# 应该返回 200 OK 和 PDF Content-Type
```

### 5. 前端验证

- ✅ 候选人详情页应该显示简历预览
- ✅ 点击简历可以下载/查看
- ✅ AI 能正确解析文件内容

## 错误处理

### 文件保存失败

如果文件保存失败，会：
1. 记录错误日志
2. 仍然创建数据库记录（fileUrl 为空）
3. 前端显示"待上传简历"

### PDF 解析失败

如果 PDF 解析失败，会：
1. 记录详细错误信息
2. 返回错误给前端
3. 不创建候选人记录

## 前端集成

### 上传请求

```typescript
const formData = new FormData();
formData.append('file', file);

const response = await fetch('http://localhost:3006/api/resume-upload/upload', {
  method: 'POST',
  body: formData,
});

const data = await response.json();
// data.data.fileUrl = '/uploads/resumes/xxx.pdf'
```

### 文件预览

```typescript
const fileUrl = `http://localhost:3006${resume.fileUrl}`;
<iframe src={fileUrl} />
<a href={fileUrl} download>下载简历</a>
```

## 后续优化建议

1. **文件存储优化**
   - 使用云存储（OSS、S3）代替本地磁盘
   - 添加文件压缩和缩略图生成
   - 定期清理未使用的文件

2. **安全性增强**
   - 添加文件名 sanitize，防止路径遍历攻击
   - 验证文件内容，防止恶意文件上传
   - 添加访问权限控制

3. **性能优化**
   - 大文件分片上传
   - 添加上传进度显示
   - 异步处理大文件解析

4. **监控和日志**
   - 添加文件上传统计
   - 记录失败原因分析
   - 定期清理孤儿文件
