你好，我们要开始执行下一阶段的开发了。根据之前的规划，目前的任务分为“系统测试与修复”、“代码清理”和“文档完善”三部分。

请按照以下顺序协助我完成工作：

### 第一阶段：代码清理 (优先执行，为测试扫清障碍)
1. **移除无用组件**：请帮我检查项目中 `StatsCard` 组件的所有引用，将其从代码库中彻底删除，并清理相关的 import 语句。
2. **统一错误处理**：检查当前的 API 调用和异步操作，找出分散的 `try-catch` 或错误提示逻辑，建议一个统一的错误处理工具函数或拦截器方案，并给出修改代码示例。
3. **代码格式化**：请给出针对本项目的 `.prettierrc` 或 `eslint` 配置建议，确保团队代码风格一致。

### 第二阶段：系统测试与 Bug 修复
1. **端到端测试方案**：针对“简历上传 -> 解析 -> 面试安排”这一核心流程，请帮我设计一套端到端（E2E）测试用例（可以使用 Cypress 或 Playwright），列出关键测试点。
2. **已知问题排查**：(此处您可以补充：*如果有具体的报错截图或现象，请在此处描述，例如：上传大文件时偶尔超时*)，请分析可能的原因并提供修复思路。
3. **性能优化建议**：针对首屏加载速度和大数据列表渲染，请给出具体优化策略（如虚拟滚动、接口缓存等）。

### 第三阶段：文档完善
1. **API 文档**：请根据项目现有的后端接口（或假设的 RESTful 规范），生成一份标准的 Markdown 格式 API 文档模板。
2. **部署文档**：请编写一份详细的 `DEPLOY.md`，包含环境依赖、构建命令、Docker 配置（如有）以及生产环境启动步骤。
3. **用户手册**：请为“面试官”和“系统管理员”两个角色，分别草拟一份简易的操作指南大纲。

---
**现在，请先从【第一阶段：代码清理】开始。**
请先告诉我如何安全地移除 `StatsCard` 组件，并给出统一错误处理的代码实现方案。